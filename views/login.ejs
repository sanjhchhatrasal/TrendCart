<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
   <div class="main h-screen w-full bg-slate-100 flex items-center justify-center">
   
        <div class="form h-[70%] w-[35%] shadow-2xl rounded-lg p-5 relative">
            <h1 class="text-3xl text-center font-semibold mt-4">Welcome to <span class="text-pink-500">TrendCart!</span></h1>
            <h2 class="text-zinc-500 mt-5 text-center">Welcome back! Log in to continue enjoying exclusive  <span class="text-pink-600">benefits</span> and <span class="text-pink-600">a seamless </span>shopping experience.</h2>
            <% if(error.length > 0){ %>
                <div class="absolute mt-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3">
                    <span class="inline-block mt-1 mb-1 text-red-600">
                        <%= error %>
                    </span>
                </div>
           <% } %>
           <form class="w-full p-2 flex flex-col items-center mt-5" action="/login" method="post">
            <input class="px-3 py-2 rounded-lg outline-none mt-3 bg-transparent w-[80%] border-2 border-zinc-300" type="email" placeholder="Email" name="email">
            <input class="px-3 py-2 rounded-lg outline-none mt-3 bg-transparent w-[80%] border-2 border-zinc-300" type="password" placeholder="Password" name="password">
            <input class="mt-5 px-3 py-2 font-semibold w-[80%] bg-pink-600 rounded-lg cursor-pointer text-white hover:bg-pink-500" type="submit" value="Login Account">
            <p class="text-zinc-500 mt-5">Don't have an account? <a class="text-pink-500 underline hover:text-pink-400" href="/">Register here</a>.</p>
           </form>
        </div>
   </div>
</body>
</html>